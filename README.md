# sentirail-slack

## Setup

Install all projec dependencies:

    npm i

## Development

Run application server:

    npm run dev-server

## Production

Run application server:

    npm start

## Usage

The available endpoints are:

* `GET /`
* `POST /`
